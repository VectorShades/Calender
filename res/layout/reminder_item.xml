<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
   
    android:gravity="center"
    android:orientation="vertical"
    android:padding="2dip" >

    <TextView
        android:id="@+id/reminder_subject"
        android:layout_width="292dp"
        android:layout_height="18dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_weight="5.17"
        android:background="@drawable/txtbackground"
        android:text="anniversary"
        android:textAlignment="center"
        android:textColor="#B9D3EE"
        android:textIsSelectable="true"
        android:textSize="14sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/reminder_Description"
        android:layout_width="290dp"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/reminder_subject"
        android:layout_below="@+id/reminder_subject"
        android:text="buy a gift for it"
        android:textAlignment="textStart"
        android:textColor="#B9D3EE"
        android:textIsSelectable="true"
        android:textSize="20sp" 
        />

</RelativeLayout>